import {{capitalize name}} from "../models/{{capitalize name}}Model.mjs";

const sevice = async () => {
    try {

    } catch (error) {
        console.error('Error fetching users:', error);
        throw error;
    }
};

export default {
    sevice
};