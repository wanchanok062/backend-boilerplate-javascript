//{{camelCase name}}Routers.mjs
import express from "express";
import {{camelCase name}}Controller from "../controllers/{{camelCase name}}Controller.mjs";
import validate from "../../Middleware/validate.mjs";
import {{camelCase name}}Schema from "../schemas/{{camelCase name}}Schema.mjs";

const router = express.Router();

/* example router.post("/", validate(userSchema.userSchemaForPost), userController.createUser); */

export default router;
